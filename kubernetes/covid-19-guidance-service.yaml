apiVersion: v1
kind: Service
metadata:
  name: covid-19-guidance
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: covid-19-guidance
  sessionAffinity: None
  type: ClusterIP
      